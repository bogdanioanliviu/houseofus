
pool:
  name: Default

steps:
- task: Bash@3
  inputs:
    filePath: '$(System.DefaultWorkingDirectory)/pipelines/extract_yq.sh'
    failOnStderr: true
    noProfile: false
  displayName: "Generate env var with new img name and prepare deploy file."

